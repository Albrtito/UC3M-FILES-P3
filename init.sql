-- Basic settings for SQL*Plus terminal
set wrap off;
set linesize 2000;
-- Change the following settings on the SQL*Plus terminal
-- set the ancho of the diseño tab to 1000

@res/NEW_creation.sql
@res/NEW_load.sql
@setup.sql
